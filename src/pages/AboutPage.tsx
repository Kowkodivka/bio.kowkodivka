import Icon from "@/components/ui/Icon";
import { siGithub } from "simple-icons";

const AboutPage = () => {
  return (
    <div class="flex items-center justify-center min-h-screen">
      <Icon icon={siGithub} class="size-6 fill-accent" />
    </div>
  );
};

export default AboutPage;
